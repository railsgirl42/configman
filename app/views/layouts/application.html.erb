<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %> | Configman </title>
    <meta name="description" content="A site for keeping track of other webiste and server configurations.">
    <meta name="author" content="Railsgirl42">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container" class="container">
      <header>
        <%= render 'layouts/navigation' %>
      </header>
      <div class="main" role="main">
        <% flash.each do |name, msg| %>
          <%= content_tag :div, msg, :id => "flash_#{name}" %>
        <% end %>
        <%= content_tag :h2, yield(:title) if show_title? %>
        <%= yield %>
      </div>
      <footer>
        <span>&copy;2012  Railsgirl42</span>
      </footer>
    </div> <!--! end of #container -->
  </body>
</html>
